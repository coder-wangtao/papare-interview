(1).确定声名值，找到「没有冲突」的样式，直接作为计算后的样式。
(2).层叠
(2.1).比较重要性
重要性从高到低:
带有 important 的作者样式
带有 important 的默认样式
作者样式
默认样式
(2.2).比较特殊性
对每个样式分别计数
| style | id | 属性 | 元素 |
| :------- | :----- | :------------------------------- | :------------------------------- |
| 是内联:1 不是内联:0 | id 选择器的数量 | 属性、类、伪类的数量 | 元素、伪元素的数量 |
(2.3).比较源次序
源码中靠后的覆盖靠前的
(3).继承
对「仍然没有值」的属性，若「可以继承」则使用继承
(4).使用默认值
对仍然没有值的属性直接使用默认值
